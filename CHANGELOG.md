# DeepSeek Codex GUI — CHANGELOG.md

## 1.0.22 — 2026-08-12

### 任务期间暂缓更新安装

- 点击“立即安装”时若仍有执行中、等待审批或排队任务，更新不会中断任务。
- 更新状态会改为“任务执行中，更新已暂缓”，并显示需要等待的任务数量。
- 所有任务结束后自动进入已下载更新的安装流程；无任务时仍会立即安装。

## 1.0.21 — 2026-08-12

### 下载完成后手动安装

- 更新下载完成后不再自动退出或重启应用。
- 右上角显示“更新已下载完成”，可选择“立即安装”或“稍后”。
- 只有点击“立即安装”时，应用才会退出并完成更新；普通关闭应用不会自动安装。

## 1.0.20 — 2026-08-12

### 更新下载速度与剩余时间

- 更新下载提示新增实时下载速度，例如 `12.4 MB/s`。
- 根据实际下载进度和速度显示预计剩余时间；速度尚未稳定时显示当前已下载大小。
- 下载完成后明确显示“下载完成 / 正在安装更新”，与自动重启流程一致。

## 1.0.19 — 2026-08-12

### 后台下载与自动重启更新

- 点击“下载更新”后立即关闭更新弹窗，用户可继续在主界面查看和操作。
- 下载期间仅在右上角显示非阻塞式进度，不再用居中弹窗遮挡主界面。
- 下载完成后不再弹出第二次确认，应用会自动退出、安装更新并重新启动。
- 补充后台下载失败提示，失败不会关闭当前应用，也不会影响当前版本继续使用。
- 已完成 1.0.18 → 1.0.19 真实跨版本更新验收，后台下载、自动安装和自动重启均成功。

## 1.0.18 — 2026-08-12

### API 识别信息与命名

- “更换 API”窗口新增当前连接信息卡，显示用户自定义名称和脱敏 Key（仅保留头部与尾部）。
- 更换 API Key 时可填写名称，名称仅作为本机识别信息保存。
- 完整 API Key 仍只写入 Codex 本机配置，不显示在界面、不上传、不重复保存。
- 安装器欢迎页、完成页和首次启动引导统一说明：安装和使用无需另行安装或开启代理软件，应用会自动完成 DeepSeek 网络连接。
- 首次启动连接检测不再向普通用户展示“代理”字段或“必要时开启代理”的旧提示。

## 1.0.17 — 2026-08-12

### 修复顶部网络状态颜色

- 修复“网络正常”在角色主题样式下被覆盖为浅色的问题。
- 网络正常保持绿色，联网失败保持红色，检测中保持琥珀色。

## 1.0.16 — 2026-08-12

### 顶部网络状态简化

- 顶部不再显示“代理”、本地端口或内置代理等实现细节，避免普通用户产生误解。
- 网络连通时显示绿色“网络正常”；无法通过当前连接访问 DeepSeek 时显示红色“联网失败”。
- 状态检测采用真实 HTTPS 连通性探测，不再只根据本地网络组件是否启动来显示成功。
- 启动检测期间显示“网络检测中”，并在连接组件退出后自动切换为“联网失败”。

## 1.0.15 — 2026-08-12

### API 更换入口修复

- 修复角色主题首页隐藏“更换 API”按钮的问题；API 设置入口现在始终显示在顶部操作区。
- 将系统原生输入框替换为与主界面一致的应用内 API 设置窗口，支持粘贴、密码保护显示、取消和 Esc 关闭。
- 保存 API Key 时显示明确的保存 / 重连状态；成功后自动触发 Codex 连接重启，失败时保留错误提示并允许重试。
- 不在界面日志、状态文本或提示框中显示完整 API Key。
- 本机安装并完成真实 GUI 验证：更换 API Key 后成功重连并发送消息。

## 1.0.14 — 2026-08-12

### 主题恢复与顶部状态初始化修复

- 修复新增顶部状态控件后，旧的 Context / Token 按钮仍插入旧 DOM 位置，导致 renderer 初始化中断的问题。
- 修复初始化中断造成的主题恢复失败：升级或重启后会继续读取用户已保存的角色主题，不再错误回退到基础深色主题。
- 顶部内置代理状态按钮、主题按钮、上下文用量按钮和 Git 审查入口统一挂载到顶部操作区。
- 本机实际安装并验证 1.0.14：悟空主题、水墨 D 图标、悟空背景、主题选择栏和内置代理状态均正常。

## 1.0.12 — Git 远程操作中心（开发中）

- 在代码审查与 Git 操作中心增加远程同步视图。
- 支持查看远程仓库、跟踪分支、领先/落后状态和本地分支列表。
- 增加获取更新、快进拉取、推送和安全切换分支。
- 推送、拉取和切换分支均需要明确确认；拉取使用 `--ff-only`，检测到冲突不会自动覆盖。

## 1.0.11 — 2026-08-11（待发布）

### 自动更新弹窗视觉统一

- 将“发现新版本”和“更新已下载”的 Windows 原生白色弹窗替换为应用内主题弹窗。
- 复用主界面的深色水墨基调、细橙色描边、胶囊按钮和 Phosphor 图标，增加紧凑的版本迁移信息条。
- 下载完成状态切换为绿色成功语义，并明确提示重启不会影响当前任务与历史记录。
- 保留后台下载进度、稍后提醒、立即重启和失败重试链路。
- 已完成 1200×780 真实应用渲染截图、关闭交互测试和视觉对比验收。

## 1.0.9 — 2026-08-11

### 代码审查与 Git 操作中心

- 顶部“审查”入口升级为完整 Git 工作区，集中显示分支、同步状态、改动、暂存、新文件与冲突数量。
- 支持逐文件查看已暂存和工作区 Diff，提供新增 / 删除着色与双列行号。
- 支持文件暂存、取消暂存和提交已暂存改动；不会自动 Push。
- 支持手动安全存档点和一键恢复，存档使用隐藏 Git ref，不污染正常提交历史。
- 撤销改动前自动建立保护存档；未跟踪文件移入 Windows 回收站，冲突与重命名文件禁止单独撤销。
- 修复 Git 状态对中文、空格文件名的识别，加入真实临时仓库回归测试。

## 1.0.3 — 2026-08-09

### NSIS 侧栏圆形标志修正

- 修正 Windows 安装欢迎页在字体缩放下将水墨 `D` 圆环显示成椭圆的问题。
- 仅对侧栏品牌标志做反向比例补偿，保留原有水墨背景、文字排版和安装流程。
- 重新打开实际安装器验证欢迎页圆度及下一步进入安装位置页的导航。

### NSIS 安装器高 DPI 资源适配

- 侧栏位图升级为 `492×942`，顶部进度图升级为 `450×171`，保持窗口布局比例不变。
- Logo、光晕和进度节点使用 3 倍源尺寸绘制，降低 125% / 150% Windows 显示缩放下的模糊。
- 重新打开实际安装器验证欢迎页、安装位置页和导航流程。

## 1.0.1 — 2026-08-09

### Codex CLI 一键安装修复

- 修复 Windows 本地 `.tgz` 组件因盘符路径导致解压失败的问题。
- 增加解压失败的详细错误提示。

## 1.0.1 — 2026-08-09

### 引导状态

- 引导完成状态绑定应用版本，升级后会重新显示首次启动引导。
- 保留用户 API 配置、权限设置、主题和历史任务。

## 2026-08-09

### 内置 Codex CLI 安装组件

- 安装包加入官方 Codex CLI Windows x64 组件。
- 首次启动可直接一键解压安装，不依赖 npm、Node.js 或临时下载。
- 安装完成后自动检测并连接 app-server。

## 2026-08-09

### 半自动 API 配置引导

- 向导会自动检查 Codex CLI 和现有 API 配置。
- 新增 API Key 粘贴、格式校验、旧配置备份、自动写入和重连测试。
- API Key 仅以密码输入框显示，不写入界面日志或状态提示。

## 2026-08-09

### 首次启动引导

- 新增安装包首次启动向导，说明 Codex CLI、DeepSeek API、连接检测和电脑访问权限设置。
- 可从向导打开 Codex 配置目录并重新检测连接。
- 引导状态保存到用户目录，完成或跳过后不重复显示；API 密钥不由应用读取或迁移。

## 2026-08-08

### Agent 运行状态

- 顶部增加当前 Turn 状态提示。
- 复用已有事件显示 Thinking、Running、执行命令、修改文件、等待审批、已完成、已停止和错误。
- 不改变 Thread、Turn、审批、Diff、停止和附件链路。

### 附件预览与批量管理

- 图片附件显示缩略图。
- 普通附件显示扩展名和大小。
- 增加附件数量、总大小和一键清空全部附件。
- 预览读取限制为图片数据，不改变附件发送路径。

### 当前执行步骤

- 增加当前任务执行步骤卡片。
- 显示 Agent 计划、完成数量、当前命令/文件操作和收尾状态。
- 保留原有活动卡片、Diff、审批和停止行为。

### 模型与推理强度设置

- 顶部模型状态入口增加设置面板。
- 支持从 app-server 获取模型列表，并选择 Low/Medium/High 推理强度。
- 设置保存到用户目录，并应用于后续 Thread/Turn。
- 当前 Turn 执行中禁止切换设置。

### 错误日志与诊断页

- 主进程持久化记录 RPC、Codex 进程、超时和发送失败。
- 日志写入用户目录并限制数量，对 API Key / Bearer Token 做脱敏。
- 顶部增加“诊断”入口，支持查看环境和最近错误、复制诊断、清空日志。

### 消息复制与剪贴板体验

- 用户消息和 Agent 回复支持悬停后一键复制。
- 保留输入框文字、图片粘贴和附件拖拽能力。
- 增加 Clipboard API 不可用时的兼容复制回退。

### 输入框右键剪贴板菜单

- 输入框右键支持复制、剪切、粘贴、删除和全选。
- 右键粘贴图片继续复用现有图片附件处理。

### 失败任务一键重试

- 失败消息旁增加“重试”按钮。
- 重试复用当前 Thread，不新建任务。
- 文字和附件均保留安全重试快照，成功后自动清理。

### 项目级默认模型

- 模型设置支持当前项目、全局默认和跟随全局默认三种范围。
- 切换项目或恢复历史任务时自动加载对应模型与推理强度。
- 项目配置按规范化绝对路径保存，不影响其他项目。

### Context / Token 使用情况

- 顶部增加上下文使用入口和百分比。
- 显示本轮输入、缓存输入、输出、推理输出、Thread 累计和模型上下文窗口。
- 仅使用 app-server 返回的真实 `thread/tokenUsage/updated` 数据。

### Codex app-server 自动重连

- app-server 退出或初始化失败后自动重连，采用指数退避并限制最长等待时间。
- 连接中断时清理挂起请求和失效审批，正在执行的 Turn 会明确显示失败。
- 重连后自动尝试恢复当前 Thread，恢复失败时下一条消息自动创建新 Thread。
- 应用退出时取消重连，避免后台残留进程或重启竞态。

### Git 状态与当前修改文件

- 顶部增加只读 Git 状态入口。
- 显示当前分支、上游同步、暂存、修改、未跟踪和冲突文件。
- 通过安全的 `execFile` 调用 Git，不执行提交、暂存、回退等写操作。
- 非 Git 仓库或 Git 未安装时显示明确提示。

### 主题皮肤系统

- 增加深色、浅色、海洋蓝、紫夜四套基础主题和 12 套七龙珠角色主题。
- 12 套角色主题分别对应悟空、克林、龟仙人、桃白白、沙鲁、兰琪、琪琪、贝吉塔、人造人 18 号、布尔玛、魔人布欧、界王。
- 主题覆盖主界面、侧边栏、聊天、附件、审批、Diff、执行卡片、诊断、Git 和 Token 面板。
- 将角色拼图拆分为 12 个项目内独立素材，避免小型精灵图带来的裁切限制和简单换色效果。
- 角色主题增加大幅主视觉、角色编号、英文签名、专属光效、玻璃卡片、标题强调色和输入区聚焦光效。
- 主题选择器升级为带当前主题预览、基础主题分组、角色卡片和独立滚动的高级面板。
- 顶部支持即时切换，主题保存到用户目录并在重启后恢复。
- 完成 1200×780、最大化窗口、角色切换、设置持久化、资源入包和打包版本重启验证。

### 七龙珠主题参考图级重制

- 按用户确认的概念图重做欢迎页构图、深蓝黑水墨背景、橙色强调、玻璃功能卡、输入区和底部主题选择坞。
- 为 12 位角色新增独立横版高清主视觉 `assets/dragonball-hero/*.png`，方形素材继续承担缩略图用途。
- 使用本地 Phosphor 图标替换欢迎页近似符号，四个快捷功能卡可点击填入任务提示。
- 底部主题坞支持悟空、贝吉塔、龟仙人、沙鲁快速切换，并可打开完整主题面板。
- 完成 1586×992 参考图并排检查、1200×780 响应式检查、主题持久化、资源入包和打包版启动验收。
- 保留聊天、附件、Thread、历史任务、审批、Diff、停止任务、诊断、Git、Token 和 DeepSeek app-server 链路。

### 欢迎标题毛笔字体匹配

- 内置 Ma Shan Zheng 中文毛笔字体及 OFL 1.1 许可，不再依赖开发机安装的系统字体。
- 重新校准欢迎标题的字号、字距、笔画厚度和橙色强调字角度，使标题更接近用户确认的参考图。
- 完成 1586×992、1200×780 和最终打包版视觉检查。
- 打包时排除开发备份和视觉验收截图，保留字体、主题素材及运行所需资源。

### 角色主题品牌头像统一

- 将角色主题左上角的蓝紫色通用 `D` 图标替换为真实水墨印章品牌素材。
- 新头像采用黑色水墨、橙色手绘圈、象牙白毛笔 `D` 和少量蓝色高光，与角色主视觉和标题笔触统一。
- 完成透明边缘、42px 小尺寸、1586×992、1200×780、资源入包和打包版启动检查。
- 保留四套基础主题、侧边栏尺寸、品牌文字和全部业务功能；Windows EXE 图标未在本次修改。

### 附件输入效率

- 输入区支持从 Windows 文件管理器拖入附件。
- 输入区支持 Ctrl+V 粘贴 PNG、JPEG 或 WebP 图片。
- 新入口复用现有附件校验、标签、发送、清理和真实读取链路。
- 重新生成并启动验证最新 Windows unpacked EXE 与 NSIS 安装包。
- 兼容浏览器、截图工具或聊天窗口拖入的无本地路径图片对象。
- 兼容浏览器或聊天窗口拖入的无本地路径 Excel、PDF、CSV、TXT 等普通文件。

### 文档治理

- 根据当前实际代码补充 `ARCHITECTURE.md`、`DEVELOPMENT_RULES.md` 和本变更记录。
- 明确 Electron 主进程、preload、renderer、持久化模块和 Codex app-server 的边界。
- 固化 Windows 中文路径、附件临时目录、备份、语法检查、启动检查和打包后重启验证规则。

### Windows 打包

- 重新生成最新的 unpacked EXE 和 NSIS 安装包。
- 验证关闭后再次启动时最近项目、历史任务、搜索、菜单和底部模型状态可以加载。
- 记录旧打包版本不会自动包含最新源码；源码修改后必须重新打包。

### 侧边栏回归修复

- 保留最近项目菜单、移除项目、历史任务菜单、重命名、删除和搜索。
- 修复历史任务区域独立滚动及底部模型状态区的布局约束。
- 将最近项目和历史任务的 `⋯` 菜单入口恢复为始终可见。

### 附件

- 附件真实进入 `turn/start`。
- 普通文件复制到 `.deepseek-codex-attachments`，图片使用 `localImage`。
- 增加类型、单文件大小、总大小校验、失败反馈和临时文件清理。

### 首页 Hero 标题参考图背景素材化

- 将固定标题「今天想开发什么？」从运行时字体渲染改为 `assets/hero-title-reference-transparent.png` 本地静态图片。
- 标题直接来自用户参考图左侧区域，保留原始毛笔字形、飞白和橙色强调，并通过透明通道与当前悟空背景融合。
- 首次欢迎页和新建任务回退模板统一引用同一张图片；响应式窗口只调整图片容器宽度。
- 保留副标题、功能卡、输入框、侧边栏、聊天、附件、Thread、审批、Diff 和停止任务逻辑。
- 完成开发窗口常规尺寸与 1200×780 视觉检查，并记录标题聚焦对照图。

## 2026-08-09

### 右侧当前任务侧边栏

- 将右侧面板调整为更接近 Codex 的当前任务侧边栏。
- 展示 Thread、项目、模型、运行状态、执行步骤、文件变更、审批和 Context 使用情况。
- 默认关闭，通过顶部按钮按需打开，避免影响主界面美观。
- 保留向当前 Thread 发送补充说明和简短追问的能力。

### 历史任务自动标题

- 首次发送消息后，根据问题内容自动生成简短历史任务标题。
- 去除常见指令前缀并按首个完整句截取，方便快速浏览。
- 手动重命名后的标题不会被自动标题覆盖。
- 已增加旧历史任务标题迁移，现有过长标题加载时也会自动压缩。
- 将自动标题长度从 24 字进一步收敛到约 16 字，避免侧边栏继续显示过长省略号。

### 顶部主题按钮样式

- 修复主题按钮被通用样式覆盖成矩形的问题。
- 统一为与模型、状态和诊断入口一致的胶囊形按钮。

### 顶部当前会话标题

- 顶部原先显示项目名称的位置改为显示当前 Thread 标题。
- 手动重命名和自动生成的会话标题保持与左侧历史任务同步。

### 电脑访问权限选择

- 输入框底部新增“每次询问 / 工作区 / 完全访问”权限选择。
- 自动记忆最后一次选择，重启后恢复。
- 当前权限随下一条任务传递给 Agent，原有审批流程继续生效。

### 主聊天发送快捷键修复

- 修复增加侧边任务输入框后，通用 `textarea` 选择器误绑定到侧边输入框的问题。
- 主聊天输入框现在重新支持 Enter 发送、Shift+Enter 换行和箭头按钮发送。

### 执行记录折叠

- 命令执行和文件修改记录默认折叠为摘要行。
- 点击记录标题即可自由展开或隐藏详细命令、输出和文件信息。
- 主对话内容保持完整显示，减少长任务会话的堆叠感。
- 执行记录集中显示在主对话上方，并增加“耗时 X 分 X 秒”摘要入口。

### 四星球任务状态指示器

- 左侧模型状态卡新增四星球状态图标。
- 执行中旋转发橙光，完成后停止并发绿光，失败显示红光。

### 执行步骤左对齐

- 执行步骤、命令记录和 Diff 统一靠左显示，不再在聊天区域居中偏右。
- 完整 Agent 回复继续显示在主聊天区，保持原有 Thread / Turn 链路。

### Thread 导出 Markdown

- 历史任务更多菜单新增“导出 Markdown”。
- 导出任务元数据、用户消息、Agent 回复、命令执行和文件修改记录。
- 未驻留历史任务导出前自动恢复 Thread，生成 UTF-8 Markdown 文件。
- 使用系统保存对话框，默认文件名来自任务标题，避免非法 Windows 文件名字符。
- 修复当前任务尚未生成历史快照时导出被 Thread 恢复流程阻塞的问题。
- 用户已验证应用重启后历史任务仍可成功导出 Markdown。
- 导出内容补充任务状态、模型设置、附件、审批、Token 使用和错误记录。

### 多 Thread 并发任务运行时

- 新增 `task-runtime-manager.js`，将单任务全局状态升级为 `Map<threadId, TaskState>`，不使用编号式第二套 Thread / Turn 变量。
- 第一版最多同时运行 2 个任务；第三个及以后进入队列，前序 Turn 完成、停止或失败后自动启动。
- app-server 的消息流、Turn、工具活动、命令输出、审批、Diff、计划、Token 和错误事件均按 `threadId` / `turnId` 路由。
- 后台任务继续运行；切换历史任务不会中断其他任务，历史列表显示执行中、排队、等待审批和失败状态。
- Stop、Approval、Diff、命令活动和任务附件临时文件按任务隔离；审批响应增加 Thread / Turn 上下文校验。
- 同一项目存在并行活动任务时显示风险提示；暂不自动创建 Git Worktree。
- app-server 重连时逐个恢复已知 Thread；无法确认恢复中的原 Turn 时明确标记失败，避免伪造执行状态。
- 新增任务运行时相关 preload IPC：任务列表、任务快照、任务切换、任务级停止和冲突提示。
- 后台切回任务时恢复原历史消息并叠加运行时快照；排队任务锁定重复发送，避免覆盖待执行内容。
- 修复审批请求与 `turn/start` 返回交错时的状态覆盖问题，确保任务继续保持“等待审批”。
- 审批响应在协议提供标识时增加 `itemId` 校验，避免并发操作的审批请求互相响应。
- 自动通过 `node --check main.js`、`node --check preload.js`、`node --check renderer.js`、运行时队列冒烟测试和 `npm start` 窗口启动检查。
- 备份：`backup-multitask-architecture-before-20260808-235445`、`backup-multitask-final-before-polish-20260809-0015`、`backup-multitask-before-approval-race-20260809-0055`、`backup-multitask-before-item-approval-check-20260809-0100`。

### 全量更新日志网页版

- 新增项目根目录 `release-notes.html`，整理当前全部 24 条重要更新记录。
- 页面沿用客户端深色水墨、橙色强调、悟空主视觉、玻璃卡片和本地毛笔字体的龙珠主题风格。
- 支持关键词搜索、类别筛选、版本筛选，以及 12 套七龙珠角色主题即时切换。
- 使用项目内本地素材，支持直接双击浏览；响应式布局适配常规桌面和窄屏窗口。
- 同时导出 `release-notes-web-v1.1.0.zip`，包含网页、字体、图标和 12 套角色视觉素材，可脱离项目目录离线浏览。
- 完成页面脚本语法、资源引用、1280×720、820×900、筛选交互、主题切换和控制台错误检查。
- 当前网页依赖项目内 `assets/` 与本地图标 CSS；完整脱离项目的离线网页包列入后续优化。

### 正式产品图标

- 最终沿用现有黑橙水墨 `D` 品牌标志，停止采用与界面不一致的新概念图标。
- 从 512×512 透明源图生成 16、20、24、32、40、48、64、128、256 像素 Windows ICO。
- 应用窗口、任务栏、EXE、安装器、卸载器和网页 favicon 使用统一品牌图标。
- 同步更新当前电脑现有的两个桌面快捷方式图标，不改变原快捷方式的启动目标和参数。
- 版本升级至 1.0.2，并重新生成 Windows NSIS 安装包。

### 内置 Codex CLI 解压兼容修复

- 打包版本固定从 `app.asar.unpacked` 读取内置 CLI，避免系统工具无法读取 Electron 虚拟归档路径。
- 移除 Windows 自带 `tar.exe` 不支持的 `--force-local` 参数。
- 解压进程改为在压缩包所在目录启动并使用纯文件名，避免 Windows 盘符路径兼容问题。
- 使用最终打包资源完成真实解压测试，确认内置 `codex.exe` 可正常释放。

### 品牌化 Windows 安装器

- 按选定方案 1 为 NSIS 安装器加入黑蓝水墨侧栏、橙色笔触、正式 `D` 品牌标志和四节点进度头图。
- 安装界面统一为深色内容区、米白文字、橙色强调，并使用 `Microsoft YaHei UI` 替换默认宋体。
- 新增品牌化欢迎页和自定义安装位置页，保留路径编辑、文件夹选择、键盘导航与 Windows 原生安装稳定性。
- 全新安装默认当前 Windows 用户；升级时保留既有安装范围。
- 安装 / 升级始终维护桌面快捷方式，同时创建开始菜单入口。
- 版本升级至 1.0.3，已实际打开最终安装器并验证欢迎页与安装位置页。
- 视觉参考、最终截图和并排对比保存在 `design-qa/`，最终验收结果为通过。
## 2026-08-10 — 项目级工作区中心第一版

- 主聊天区新增按需打开的项目工作区侧栏。
- 支持查看项目文件、搜索文件路径、查看最近修改文件，并用系统默认程序打开文件。
- 工作区访问限制在当前项目目录内，避免越界访问。
## 2026-08-10 — GitHub Actions 自动发布

- 新增标签触发的 Windows 自动构建与 GitHub Release 发布流程。
- 构建时自动下载并验证固定版本的 Codex CLI Windows 内置组件。
- 自动上传 NSIS 安装包、blockmap 和 latest.yml，供客户端自动更新。
- 修复标签构建时 electron-builder 隐式发布导致缺少 GH_TOKEN 的失败，改为构建与上传职责分离。
- `v1.0.8` 已通过 GitHub Actions 真实发布验证，三个自动更新资产均可在 Release 获取。
## 2026-08-11

- Git 审查中心在普通文件夹中不再显示黄色感叹号；检测到尚未初始化 Git 时，会提供明确的一键初始化引导。
- 初始化前会说明只创建 `.git` 文件夹，不删除、不上传项目文件，成功后自动刷新状态。
# 变更记录

## 1.0.17 — 2026-08-12
### 修复顶部网络状态颜色
- 修复“网络正常”在角色主题样式下被覆盖为浅色的问题。
- 网络正常保持绿色，联网失败保持红色，检测中保持琥珀色。
