# 聊天框拉高后首页内容被遮挡修复

## 根因

主界面使用纵向 Flex 布局，但 `.chat` 没有明确允许收缩，聊天框高度增加后，首页内容与底部输入区发生视觉重叠。

## 修复

- `.chat` 增加 `min-height: 0`，保证内容区域可以在 Flex 布局中收缩并独立滚动。
- `.composer-wrap` 增加固定 Flex 边界，始终作为底部区域占用自身高度。
- 保留输入框高度记忆、自适应限制和所有聊天功能。

备份目录：`backup-composer-overlap-before-20260808-153548`

